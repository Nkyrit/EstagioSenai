sudo apt install net-tools

ifconfig

route -n ( Informações de Endereços)

Inet4 : 192.168.194.136
GW: 192.168.194.2
DNS: 8.8.8.8

ls

cd /etc

ls

cd netplan

ls

sudo nano 01-network-manager-all.yaml

(Escreva na exata ordem e espaçamentos)

network:
  version: 2
  renderer: networkd 
  ethernets:
    ens33:
      dhcp4: no
      addresses:
        - 192.168.194.136/24 ( Seu ip /24)
      routes:
        - to: default
          via: 192.168.194.2 ( Seu Gateway)
      nameservers:
        addresses:
          - 8.8.8.8
          - 8.8.4.4

CRTL O (Para salvar o arquivo) Enter (Mantenha o mesmo nome)
CRTL X (Para fechar o arquivo)

sudo netplan apply

Em caso de aviso de permissão:

sudo chmod 600 /etc/netplan/01-network-manager-all.yaml

sudo netplan apply

ip route

Caso tenham duas rotas ativas use:

systemctl status systemd-networkd

Se estiver desativado use:
sudo systemctl enable systemd-networkd --now

Após isso use esses comandos:

sudo systemctl stop NetworkManager

sudo systemctl disable NetworkManager

sudo systemctl start systemd-networkd

sudo systemctl enable systemd-networkd

sudo netplan apply

Verifique:

ip a
ip Route

Para fixar o static quando reiniciar a máquina, entre também no arquivo:

sudo nano 50-cloud-init.yaml

e altere o Dhcp4 para False.

Para testar:

ip a
ip route
ping 192.168.194.2
ping 8.8.8.8

Reinicie a máquina e verifique se continua com as novas configurações.
